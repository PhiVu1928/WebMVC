<div class="navbar-custom-menu">
    <ul class="nav navbar-nav">
        <!-- User Account: style can be found in dropdown.less -->
        <li class="dropdown user user-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <label href="#">phi vũ</label>
            </a>
            <ul class="dropdown-menu">
                <!-- User image -->
                <li class="user-header">
                    <img src="~/Assets/Client/images/TB_logo.png">
                    @{
                        var user = (BanVotMVC.Common.UserLogin)Session[BanVotMVC.Common.CommonConstants.USER_SESSION];
                    }
                    @if (user != null)
                    {
                        <p>
                            @user.UserName 
                            <small>Quyền quản trị: @user.GroupID</small>
                        </p>
                    }
                </li>
                <!-- Menu Footer-->
                <li class="user-footer">
                    <div class="pull-right">
                        @Html.ActionLink("Đăng xuất", "Logout", "Home", new { @class = "btn btn-default btn-flat" })
                        </div>
                </li>
            </ul>
        </li>
        <!-- Control Sidebar Toggle Button -->
    </ul>
</div>
